version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    volumes:
      - ./src:/app/src
      - ./web:/app/web
      - ./logs:/app/logs
      - ./data:/app/data
    command: pnpm dev
    ports:
      - "3000:3000"
      - "5173:5173"
